#include <iostream>
using namespace std;

int main(){
    cout << "Hello world" << endl; // printing only one string and adding a new line after it using endl (we can also use "\n" for doing the same thing instead of endl)
    cout << "Hello world" << " second string"; // printing second string in the same line
}