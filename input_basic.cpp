#include <iostream>
using namespace std;
int main(){
    int x;
    cin >> x; // Taking input for one variable

    cout<<"The input given (stored in x) is "<<x;

    int a,b;
    cin >> a >> b; // Taking input for two variables

    return 0;
}